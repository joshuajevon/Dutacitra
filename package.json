{
    "private": true,
    "type": "module",
    "scripts": {
        "install:env": "composer install && php artisan key:generate && npm i && npm run dev",
        "dev": "vite",
        "build": "vite build",
        "watch": "npx tailwindcss -i ./resources/css/app.css -o ./public/css/build.css --watch"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.16",
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.8.0",
        "postcss": "^8.4.31",
        "tailwindcss": "^3.3.3",
        "vite": "^4.0.0"
    }
}
